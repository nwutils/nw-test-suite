{
  "name": "nw-api-sanity-check",
  "main": "index.html",
  "private": true,
  "scripts": {
    "format": "prettier --write ./**/*",
    "lint": "eslint --config=eslint.config.cjs --fix ./**/*.js",
    "manual": "nw .",
    "start": "node --test-reporter=spec --test nw chrome"
  },
  "devDependencies": {
    "eslint": "^8.47.0",
    "nw": "0.78.0-sdk",
    "prettier": "^3.0.0",
    "selenium-webdriver": "^4.10.0"
  },
  "engines": {
    "node": ">=20.3.0",
    "npm": ">=9.8.1"
  }
}
